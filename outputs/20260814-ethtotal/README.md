# EthTotal 01e

Reviewer entry point: [`01e_PARTIAL_ethtotal.json`](./01e_PARTIAL_ethtotal.json)

This directory intentionally contains one 01e file. It is replaced by the
latest validated run. Optional judge evidence is under `quality/`; previous
versions are recoverable from git history.
